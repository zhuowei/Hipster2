import {Container, Button, Tab, Tabs, TextField} from '@material-ui/core';
import SearchHeader from './SearchHeader';
function SearchClubPage() {
  return <SearchHeader currentPage={1} />;
}
export default SearchClubPage;
